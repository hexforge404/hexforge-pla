{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://hexforge.internal/pla/v2/device_time_sync.schema.json",
  "title": "Device Time Synchronization",
  "description": "Brain-authoritative host-to-device UTC synchronization anchor for one serial session.",
  "type": "object",
  "required": ["type", "sync_id", "utc_anchor"],
  "properties": {
    "type": {
      "const": "time_sync"
    },
    "sync_id": {
      "type": "string",
      "description": "Fresh 128-bit identifier generated by the Brain for this serial synchronization session.",
      "pattern": "^[0-9a-f]{32}$"
    },
    "utc_anchor": {
      "type": "string",
      "description": "Canonical UTC RFC3339 timestamp with millisecond precision supplied by the Brain.",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$"
    }
  },
  "additionalProperties": false
}
